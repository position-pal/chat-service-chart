# Base informations for the service image
image:
  repository: positionpal/chat-service
  tag: "3.8.0@sha256:ccb1c7ef2772c8abca4312c01870205d102f24e2b2e44bf4fd3ce35535f21f41"
  pullPolicy: IfNotPresent

logLevel: INFO
serviceName: chatservice
replicaNumber: 2
requiredContactPointNr: 2

managementPort: 8558
httpPort: 8080
grpcPort: 5052

akkaLicenseKey: ""

resources:
  limits:
    cpu: "1"
    memory: "512Mi"
  requests:
    cpu: "0.5"
    memory: "256Mi"

# Configure the connection information for the rabbtimq service
rabbitmq:
  username: user
  password: ""
  namespace: rabbitmq
  serviceName: rabbitmq
  virtualHost: /
  userServiceEventQueue: group_updates_chat_service
  port: 5672

# Configure the connection information for the cassandra service
cassandra:

  image:
    debug: true
 
  dbUser:
    user: admin
    password: password

  replicaCount: 2
  initDBConfigMap: script-db-configmap
